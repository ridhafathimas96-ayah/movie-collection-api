# Movie Collection Display API

A Node.js + Express API that displays movies using 3 endpoints with a frontend webpage.

## Endpoints
- GET /movies → All movies
- GET /movies/classics → Classic movies
- GET /movies/genres → All genres with count

## How to run
npm install
npm start

Visit: http://localhost:3000
